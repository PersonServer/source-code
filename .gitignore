# Build output
/target
**/*.rs.bk
*.pdb

# Operator secrets and state — never commit. The keys file holds the signing
# keys and the pairwise secret; the database holds person records, bindings,
# consent history and directed identifiers (personal data).
psd-keys.json
psd-keys.json.tmp
psd.db
psd.db-shm
psd.db-wal
psd.json
psd.*.json
!docs/psd.example.json
audit.log
*.audit.jsonl

# Jekyll (personserver.dev is built from this tree by GitHub Pages)
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/

# Editors and OS
.DS_Store
*.swp
.idea/
.vscode/
