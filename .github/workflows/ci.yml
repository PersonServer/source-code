name: ci

# Adapted from apd (MIT OR Apache-2.0), github.com/AgentProvider/source-code.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  rust:
    name: rust (fmt · clippy · test)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets -- -D warnings
      # The suite is hermetic: mock Agent Provider, resource and Access
      # Server run in-process. Nothing here needs the network.
      - run: cargo test --workspace

  helm:
    name: helm (lint · template)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
        with:
          version: v3.16.2
      - run: helm lint charts/psd
      - run: helm lint charts/psd -f charts/psd/ci/dev-values.yaml
      # Render with CI values and validate every doc is well-formed YAML, so a
      # malformed template fails here rather than in a user's cluster.
      - run: |
          helm template t charts/psd -f charts/psd/ci/dev-values.yaml > /tmp/rendered.yaml
          python3 -c "import sys,yaml; list(yaml.safe_load_all(open('/tmp/rendered.yaml')))"

  docker:
    name: docker (build check)
    # On main pushes the edge workflow builds + pushes multi-arch; here we only
    # need a fast build check on PRs.
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      # amd64 only for speed; edge and release build multi-arch.
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
