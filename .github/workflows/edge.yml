name: edge

# Continuous delivery: every push to main publishes a fresh installable
# "edge" image and OCI Helm chart, so there is always something installable
# between tagged releases.
#   image:  ghcr.io/<owner>/psd:edge  (+ :main-<sha>)
#   chart:  oci://ghcr.io/<owner>/charts/psd  version <appVersion>-edge.<run>
# Adapted from apd (MIT OR Apache-2.0), github.com/AgentProvider/source-code.
on:
  push:
    branches: [main]

concurrency:
  group: edge
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@v5
        with:
          # metadata-action lowercases the image name itself; GHCR rejects
          # mixed case and the org login is "PersonServer".
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/psd
          tags: |
            type=raw,value=edge
            type=sha,prefix=main-
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  chart:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
        with:
          version: v3.16.2
      - name: Package and push edge OCI chart
        run: |
          set -euo pipefail
          # OCI references must be lowercase; the org login name is mixed-case.
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          APP=$(awk '/^appVersion:/{gsub(/"/,"",$2); print $2}' charts/psd/Chart.yaml)
          V="${APP}-edge.${GITHUB_RUN_NUMBER}"
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login "$REGISTRY" \
            --username "${{ github.actor }}" --password-stdin
          # app-version "edge" makes the chart default image.tag to :edge.
          helm package charts/psd --version "$V" --app-version edge -d dist
          helm push "dist/psd-${V}.tgz" "oci://${REGISTRY}/${OWNER}/charts"
          echo "Pushed chart oci://${REGISTRY}/${OWNER}/charts/psd:${V}"
