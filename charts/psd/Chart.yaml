apiVersion: v2
name: psd
description: A self-hostable AAuth Person Server — decides which AI agents may act for a person, at which services, on what terms
type: application
# version (chart) and appVersion (image tag) are overwritten in CI from the
# git tag; these are the local/dev defaults.
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.23.0-0"
home: https://personserver.dev
sources:
  - https://github.com/PersonServer/source-code
keywords:
  - aauth
  - person-server
  - authorization
  - consent
  - ai-agents
  - passkeys
maintainers:
  - name: personserver
    url: https://github.com/PersonServer
annotations:
  category: Security
  licenses: MIT
